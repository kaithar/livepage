<?

/**
 * Set this to 1 if you intend to run more than one site off this install (ie, more than one database)
 */
$config['vhosts'] = 1;

/**
 * If this is set to a message, all sites will be disabled and the message will be displayed.
 */
$config['disabled'] = 0;

/**
 * The following config variables should only be set -here- if you set vhosts to 0.
 * Otherwise you need to go and setup the config files in domains/
 */

/**
 * Uncomment and set this one to the domain name you're running the site on.
 * Yes, you still need this, it's important!
 */
// $config['domain'] = "";

/**
 * Database connection stuff...
 */
//$config['db_name'] = "";
//$config['db_user'] = "";
//$config['db_pass'] = "";

?>
